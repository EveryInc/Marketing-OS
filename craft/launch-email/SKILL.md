# Launch Email

Writes launch announcement emails for product releases and updates.

## When to invoke

- When drafting an email for a product launch, feature release, or improvement
- When a launch skill orchestrates channel execution and needs the email deliverable
- When writing a standalone product announcement email

## Dependencies

- `foundation/marketing-os`
- `brand-voice/{relevant brand}` (loaded dynamically)
- `positioning/{relevant brand}` (loaded dynamically)
- `launches/{parent launch skill}` (when invoked inside a launch)

## To do

Populate on completion of the launch email craft playbook.
