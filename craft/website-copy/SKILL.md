# Website Copy

Writes website copy — landing pages, product pages, feature sections, and CTAs.

## When to invoke

- When drafting or updating website copy for a product or brand
- When a launch skill orchestrates channel execution and needs the web deliverable
- When writing a standalone landing page or product page

## Dependencies

- `foundation/marketing-os`
- `brand-voice/{relevant brand}` (loaded dynamically)
- `positioning/{relevant brand}` (loaded dynamically)
- `launches/{parent launch skill}` (when invoked inside a launch)

## To do

Populate on completion of the website copy craft playbook.
