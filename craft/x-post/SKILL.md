# X Post

Writes posts for X (Twitter) for launches, engagement, and brand presence.

## When to invoke

- When drafting an X post for a launch or announcement
- When a launch skill orchestrates channel execution and needs the X deliverable
- When writing a standalone X post or thread for a brand

## Dependencies

- `foundation/marketing-os`
- `brand-voice/{relevant brand}` (loaded dynamically)
- `positioning/{relevant brand}` (loaded dynamically)
- `launches/{parent launch skill}` (when invoked inside a launch)

## To do

Populate on completion of the X post craft playbook.
