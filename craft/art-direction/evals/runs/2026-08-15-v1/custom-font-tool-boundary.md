Do not replace the brand fonts. Keep Figma as the owner of canonical geometry, variables,
components, and portrait placement. Use a hybrid renderer or a local Figma plugin for
native text insertion when that path can access the approved typefaces. Record which tool
owns typography and export, and keep a human-finished fallback if neither path preserves
the font reliably.

Return the generated object to Figma, confirm the expected layers and dimensions, then
render it at intended size and compare line breaks, metrics, spacing, crop, and export.
