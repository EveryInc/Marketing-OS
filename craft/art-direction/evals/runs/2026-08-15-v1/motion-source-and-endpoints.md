Inspect the graph before another run and preview the connected source. Confirm that the
wordmark, not the decorative reference, is the motion subject.

Build exact `ME`, `WE`, and `Every` frames with matching canvas, type metrics, baseline,
background, and crop. Generate `ME` to `WE`, then reuse the approved `WE` endpoint as the
exact start frame for `WE` to `Every`. If the model still alters letterforms, symbols,
coordinates, or timing, move the sequence to deterministic motion rather than prompting
around the drift.
