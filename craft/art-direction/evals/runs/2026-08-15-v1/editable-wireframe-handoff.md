Treat the Google Doc as a locked manifest: exact copy, omissions, placeholders,
annotations, and section order. Do not invent helper headlines, labels, or sample content;
flag any apparent copy gap for the owner.

Build the wireframe as editable Figma frames and layers because the next operator will
continue production there. Represent the FAQ as a component or prototype with one defined
open state and the remaining items collapsed. After import, compare every string and
placeholder with the manifest, edit a text layer to prove editability, inspect the expected
structure, and run the accordion interaction in the live file. A PDF may accompany the
handoff as a review proof, but it is not the canonical deliverable.
