“More premium, dynamic, editorial, and modern” changes too many dimensions to produce a
useful comparison. Branch from the strongest poster and lock its copy, composition, crop,
type scale, references, source assets, seed, aspect ratio, and model settings.

Choose the first decision: palette or texture. For example, preserve everything else and
replace only the warm palette with the approved cool palette. Record the parent, locked
inputs, changed variable, visible critique, and verdict. If that branch wins, use it as the
parent for a separate texture test.
