# LinkedIn Post

Writes LinkedIn posts for product launches, thought leadership, and brand building.

## When to invoke

- When drafting a LinkedIn post for a launch or announcement
- When a launch skill orchestrates channel execution and needs the LinkedIn deliverable
- When writing a standalone LinkedIn post for a brand

## Dependencies

- `foundation/marketing-os`
- `brand-voice/{relevant brand}` (loaded dynamically)
- `positioning/{relevant brand}` (loaded dynamically)
- `launches/{parent launch skill}` (when invoked inside a launch)

## To do

Populate on completion of the LinkedIn post craft playbook.
